import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cheems.angular',
  appName: 'CheemsAngular',
  webDir: 'dist/cheems-angular/browser'
};

export default config;
